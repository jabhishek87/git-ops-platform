apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: worker-redis
  namespace: novadeploy
spec:
  refreshInterval: 1m
  secretStoreRef:
    name: vault-backend
    kind: SecretStore
  target:
    name: worker-redis-credentials
    creationPolicy: Owner
  data:
    - secretKey: host
      remoteRef:
        key: dev/worker/redis
        property: host
    - secretKey: port
      remoteRef:
        key: dev/worker/redis
        property: port
    - secretKey: password
      remoteRef:
        key: dev/worker/redis
        property: password
